// Ambient declarations for global side-effect style imports (e.g. `import './global.css'`
// in the App Router root layout). Kept under src/ so the app tsconfig `include` picks it up.
declare module '*.css';
